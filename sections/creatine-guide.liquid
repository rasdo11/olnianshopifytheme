{% liquid
  assign guide_product = section.settings.product
  assign guide_variant = guide_product.selected_or_first_available_variant
  assign offer_active = false
  assign offer_code = section.settings.discount_code | strip
  if section.settings.offer_enabled and offer_code != blank and section.settings.discount_percent > 0
    assign offer_active = true
  endif
  if customer and customer.orders_count > 0
    assign offer_active = false
  endif
  assign price = guide_variant.price
  if offer_active
    assign discount_multiplier = 100 | minus: section.settings.discount_percent
    assign price = price | times: discount_multiplier | divided_by: 100.0 | round
  endif
  assign form_id = 'GuideForm-' | append: section.id
  assign return_url = routes.cart_url
  if offer_active
    assign encoded_code = offer_code | url_encode
    assign return_url = routes.root_url | append: 'discount/' | append: encoded_code | append: '?redirect=' | append: routes.cart_url
  endif
  assign review_count = 0
  for block in section.blocks
    if block.type == 'testimonial' and block.settings.quote != blank and block.settings.author != blank
      assign review_count = review_count | plus: 1
    endif
  endfor
%}
<div class="crg" id="Guide-{{ section.id }}">
  <div class="crg-welcome"><button type="button" data-email-open aria-haspopup="dialog" aria-controls="GuideEmail-{{ section.id }}">{{ 'creatine_guide.email.trigger' | t }} <span aria-hidden="true">↗</span></button></div>
  <header class="crg-nav crg-wrap">
    <a class="crg-logo" href="{{ routes.root_url }}" aria-label="{{ 'creatine_guide.nav.home' | t }}">{{ 'creatine_guide.nav.brand' | t }}</a>
    <nav aria-label="{{ 'creatine_guide.nav.label' | t }}">
      <a href="#GuideLearn-{{ section.id }}">{{ 'creatine_guide.nav.learn' | t }}</a>
      <a href="#GuideFormula-{{ section.id }}">{{ 'creatine_guide.nav.formula' | t }}</a>
    </nav>
    <a class="crg-nav-cta" href="#GuideOffer-{{ section.id }}">{{ 'creatine_guide.nav.shop' | t }} <span aria-hidden="true">↗</span></a>
  </header>
  <section class="crg-hero crg-wrap" aria-labelledby="GuideHeading-{{ section.id }}"><div class="crg-hero-copy"><p class="crg-eyebrow">{{ 'creatine_guide.hero.eyebrow' | t }}</p><h1 id="GuideHeading-{{ section.id }}">{% if section.settings.heading != blank %}{{ section.settings.heading | escape }}{% else %}{{ 'creatine_guide.hero.heading' | t }}{% endif %}</h1><p class="crg-hero-lead">{{ 'creatine_guide.hero.body' | t }}</p></div></section>
  <section class="crg-tree crg-space crg-wrap" id="GuideFit-{{ section.id }}" aria-labelledby="GuideFitTitle-{{ section.id }}">
    <h2 class="visually-hidden" id="GuideFitTitle-{{ section.id }}">{{ 'creatine_guide.tree.heading' | t }}</h2>
    <div class="crg-tree-interactive" hidden data-tree>
      <div class="crg-tree-choices" role="group" aria-label="{{ 'creatine_guide.tree.heading' | t }}">
        {% assign concerns = 'strength,active,plant,focus' | split: ',' %}
        {% for concern in concerns %}{% assign concern_key = 'creatine_guide.tree.' | append: concern %}
          {% assign image_key = concern | append: '_image' %}
          <button type="button" data-tree-choice="{{ concern }}" aria-controls="GuideTreeResult-{{ section.id }}" aria-pressed="false">{% if section.settings[image_key] != blank %}{{ section.settings[image_key] | image_url: width: 800 | image_tag: widths: '240, 400, 600, 800', sizes: '(min-width: 750px) 23vw, 46vw', loading: 'eager', alt: '' }}{% endif %}<span class="crg-card-copy">{{ concern_key | t }}</span><span class="crg-tree-arrow" aria-hidden="true">↗</span></button>
        {% endfor %}
      </div>
      <div class="crg-tree-connector" aria-hidden="true"></div>
      <div class="crg-tree-result" id="GuideTreeResult-{{ section.id }}" data-tree-result aria-live="polite" aria-atomic="true"><div class="crg-tree-placeholder"><span aria-hidden="true">↓</span><h3>{{ 'creatine_guide.tree.prompt' | t }}</h3><p>{{ 'creatine_guide.tree.hint' | t }}</p></div></div>
      <div class="crg-tree-more" data-tree-more hidden><p>{{ 'creatine_guide.tree.other' | t }}</p><div role="group" aria-label="{{ 'creatine_guide.tree.other' | t }}"><button type="button" data-tree-choice="strength" aria-controls="GuideTreeResult-{{ section.id }}" aria-pressed="false">{{ 'creatine_guide.tree.strength_short' | t }} ↗</button><button type="button" data-tree-choice="active" aria-controls="GuideTreeResult-{{ section.id }}" aria-pressed="false">{{ 'creatine_guide.tree.active_short' | t }} ↗</button><button type="button" data-tree-choice="plant" aria-controls="GuideTreeResult-{{ section.id }}" aria-pressed="false">{{ 'creatine_guide.tree.plant_short' | t }} ↗</button></div></div>
    </div>
    {% assign paths = 'strength,active,plant,focus' | split: ',' %}{% for path in paths %}
      {% assign title_key = 'creatine_guide.tree.' | append: path | append: '_title' %}{% assign body_key = 'creatine_guide.tree.' | append: path | append: '_body' %}{% assign limit_key = 'creatine_guide.tree.' | append: path | append: '_limit' %}{% assign source_key = 'creatine_guide.tree.' | append: path | append: '_source' %}
      <template data-tree-template="{{ path }}"><div class="crg-tree-answer"><div><p class="crg-eyebrow">{{ 'creatine_guide.tree.result_label' | t }}</p><h3 tabindex="-1">{{ title_key | t }}</h3><p class="crg-body">{{ body_key | t }}</p><a class="crg-text-link" href="#GuideFormula-{{ section.id }}">{{ 'creatine_guide.tree.why' | t }} ↗</a></div><aside><p class="crg-eyebrow">{{ 'creatine_guide.tree.limit_label' | t }}</p><p>{{ limit_key | t }}</p><a class="crg-text-link" href="{{ source_key | t }}" target="_blank" rel="noopener">{{ 'creatine_guide.tree.source' | t }} ↗</a></aside></div></template>
    {% endfor %}
    <noscript><p class="crg-body">{{ 'creatine_guide.tree.nojs' | t }}</p></noscript><p class="crg-fine crg-tree-scope">{{ 'creatine_guide.tree.scope' | t }}</p>
  </section>
  <section class="crg-learn crg-space" id="GuideLearn-{{ section.id }}" aria-labelledby="GuideLearnTitle-{{ section.id }}">
    <div class="crg-wrap">
      <div class="crg-split">
        <div>
          <p class="crg-eyebrow">{{ 'creatine_guide.learn.eyebrow' | t }}</p>
          <h2 id="GuideLearnTitle-{{ section.id }}">{{ 'creatine_guide.learn.heading' | t }}</h2>
          <p class="crg-body">{{ 'creatine_guide.learn.intro' | t }}</p>
          <p class="crg-body">{{ 'creatine_guide.learn.body' | t }}</p>
        <details class="crg-details">
          <summary>{{ 'creatine_guide.science.link' | t }}<span aria-hidden="true">+</span></summary>
          <div><p>{{ 'creatine_guide.science.body' | t }}</p><ul><li><a href="https://ods.od.nih.gov/factsheets/ExerciseAndAthleticPerformance-HealthProfessional/" target="_blank" rel="noopener">{{ 'creatine_guide.science.study1' | t }}</a></li><li><a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC5469049/" target="_blank" rel="noopener">{{ 'creatine_guide.science.study2' | t }}</a></li></ul><p>{{ 'creatine_guide.science.limit' | t }}</p></div>
        </details>
        </div>
        {% if section.settings.lifestyle_image != blank %}
          <figure class="crg-lifestyle">{% assign lifestyle_alt = 'creatine_guide.learn.photo_alt' | t %}{{ section.settings.lifestyle_image | image_url: width: 1100 | image_tag: widths: '400, 650, 900, 1100', sizes: '(min-width: 750px) 45vw, 90vw', loading: 'lazy', alt: lifestyle_alt }}<figcaption>{{ 'creatine_guide.learn.photo_caption' | t }}</figcaption></figure>
        {% endif %}
      </div>
    </div>
  </section>
  <section class="crg-month crg-space" id="GuideFormula-{{ section.id }}" aria-labelledby="GuideFormulaTitle-{{ section.id }}">
    <div class="crg-wrap">
      <p class="crg-eyebrow">{{ 'creatine_guide.month.eyebrow' | t }}</p>
      <h2 id="GuideFormulaTitle-{{ section.id }}">{{ 'creatine_guide.month.heading' | t }}</h2>
      <p class="crg-body">{{ 'creatine_guide.month.intro' | t }}</p>
      <div class="crg-timeline">
        {% for day in (1..3) %}{% assign title_key = 'creatine_guide.month.title' | append: day %}{% assign body_key = 'creatine_guide.month.body' | append: day %}{% assign day_key = 'creatine_guide.month.day' | append: day %}
          <article><span class="crg-day">{{ day_key | t }}</span><div class="crg-progress" aria-hidden="true"><i style="width:{{ day | times: 33 }}%"></i></div><h3>{{ title_key | t }}</h3><p>{{ body_key | t }}</p></article>
        {% endfor %}
      </div>
      <p class="crg-fine">{{ 'creatine_guide.month.science' | t }}</p>
      <div class="crg-clean"><h3>{{ 'creatine_guide.proof.heading' | t }}</h3><div>{% for item in (1..3) %}{% assign clean_key = 'creatine_guide.proof.clean' | append: item %}<p><span aria-hidden="true">✓</span> {{ clean_key | t }}</p>{% endfor %}</div></div>
      {% if section.settings.facts_image != blank %}<details class="crg-details crg-facts-disclosure"><summary>{{ 'creatine_guide.proof.facts' | t }}<span aria-hidden="true">+</span></summary><div>{% assign facts_alt = 'creatine_guide.proof.alt' | t %}{{ section.settings.facts_image | image_url: width: 1000 | image_tag: loading: 'lazy', alt: facts_alt }}</div></details>{% endif %}
    {% if review_count > 0 %}
      <div class="crg-reviews"><h3>{{ 'creatine_guide.proof.reviews' | t }}</h3><div class="crg-review-grid">{% for block in section.blocks %}{% if block.type == 'testimonial' and block.settings.quote != blank and block.settings.author != blank %}<figure {{ block.shopify_attributes }}><blockquote>{{ block.settings.quote | escape }}</blockquote><figcaption>{{ block.settings.author | escape }}</figcaption>{% if block.settings.source_url != blank %}<a class="crg-text-link" href="{{ block.settings.source_url }}">{{ block.settings.source_label | escape }}</a>{% endif %}</figure>{% endif %}{% endfor %}</div></div>
    {% endif %}    </div>
  </section>
  <section class="crg-offer crg-space" id="GuideOffer-{{ section.id }}" aria-labelledby="GuideOfferTitle-{{ section.id }}">
    <div class="crg-wrap crg-offer-grid">
      <div class="crg-offer-image">{% assign offer_image = section.settings.offer_image | default: guide_product.featured_image %}{% if offer_image != blank %}{{ offer_image | image_url: width: 1000 | image_tag: widths: '400, 600, 800, 1000', sizes: '(min-width: 900px) 45vw, 90vw', loading: 'lazy', alt: guide_product.title }}{% endif %}</div>
      <div class="crg-offer-copy">
        <p class="crg-eyebrow">{{ 'creatine_guide.offer.eyebrow' | t }}</p><h2 id="GuideOfferTitle-{{ section.id }}">{{ 'creatine_guide.offer.heading' | t }}</h2><p class="crg-body">{{ 'creatine_guide.offer.body' | t }}</p>
        {% if guide_product != blank %}
          {% if offer_active %}<p class="crg-offer-badge">{{ 'creatine_guide.offer.badge' | t: percent: section.settings.discount_percent }}</p>{% endif %}
          <h3>{{ 'creatine_guide.offer.name' | t }}</h3><p class="crg-fine">{{ 'creatine_guide.offer.size' | t }}</p>
          <div class="crg-price" aria-live="polite">
            {% if offer_active %}<s><span class="visually-hidden">{{ 'creatine_guide.offer.regular' | t }}</span><span data-guide-original>{{ guide_variant.price | money }}</span></s>{% endif %}
            <strong><span class="visually-hidden">{% if offer_active %}{{ 'creatine_guide.offer.eligible_price' | t }}{% else %}{{ 'creatine_guide.offer.price_label' | t }}{% endif %}</span><span data-guide-price>{{ price | money }}</span></strong>
          </div>
          {% unless guide_product.requires_selling_plan %}<p class="crg-one-time">{{ 'creatine_guide.offer.one_time' | t }}</p>{% endunless %}
          {% if guide_product.requires_selling_plan %}
            <a class="crg-button" href="{{ guide_product.url }}">{{ 'creatine_guide.offer.subscription' | t }}</a>
          {% else %}
            {% form 'product', guide_product, id: form_id, return_to: return_url, class: 'crg-product-form' %}
              {{ form.errors | default_errors }}
              {% if guide_product.variants.size > 1 %}
                <label for="GuideVariant-{{ section.id }}">{{ 'creatine_guide.offer.variant' | t }}</label>
                <select id="GuideVariant-{{ section.id }}" name="id" data-guide-variant>
                  {% for variant in guide_product.variants %}
                    {% assign option_price = variant.price %}{% if offer_active %}{% assign option_price = option_price | times: discount_multiplier | divided_by: 100.0 | round %}{% endif %}{% assign option_serving = option_price | divided_by: 30.0 | round | money %}
                    <option value="{{ variant.id }}" data-price="{{ option_price | money | escape }}" data-original="{{ variant.price | money | escape }}" data-serving="{{ 'creatine_guide.offer.per_day' | t: amount: option_serving | escape }}" {% unless variant.available %}disabled{% endunless %} {% if variant.id == guide_variant.id %}selected{% endif %}>{{ variant.title | escape }} — {{ option_price | money }}{% unless variant.available %} — {{ 'creatine_guide.offer.sold_out' | t }}{% endunless %}</option>
                  {% endfor %}
                </select>
              {% else %}<input type="hidden" name="id" value="{{ guide_variant.id }}">{% endif %}
              <input type="hidden" name="quantity" value="1">
              <button class="crg-button crg-buy" type="submit" {% unless guide_variant.available %}disabled{% endunless %}>{% unless guide_variant.available %}{{ 'creatine_guide.offer.sold_out' | t }}{% else %}{% if offer_active %}{{ 'creatine_guide.offer.discount_button' | t }}{% else %}{{ 'creatine_guide.offer.button' | t }}{% endif %}<span aria-hidden="true">↗</span>{% endunless %}</button>
            {% endform %}
          {% endif %}
          {% if offer_active %}<p class="crg-fine crg-terms">{{ 'creatine_guide.offer.terms' | t }}</p>{% endif %}
          <p class="crg-fine crg-shipping">{{ 'creatine_guide.offer.shipping' | t }}</p><a class="crg-text-link" href="{{ guide_product.url }}">{{ 'creatine_guide.offer.details' | t }} ↗</a>
        {% else %}<p>{{ 'creatine_guide.offer.unavailable' | t }}</p>{% if request.design_mode %}<p>{{ 'creatine_guide.offer.editor' | t }}</p>{% endif %}{% endif %}
      </div>
    </div>
  </section>
  {% assign email_form_id = 'GuideEmailForm-' | append: section.id %}
  <dialog class="crg-email-dialog" id="GuideEmail-{{ section.id }}" aria-labelledby="GuideEmailTitle-{{ section.id }}" aria-describedby="GuideEmailIntro-{{ section.id }}">
    <button type="button" class="crg-email-close" data-email-close aria-label="{{ 'creatine_guide.email.close' | t }}">×</button>
    <p class="crg-eyebrow">{{ 'creatine_guide.email.eyebrow' | t }}</p><h2 id="GuideEmailTitle-{{ section.id }}">{{ 'creatine_guide.email.heading' | t }}</h2><p class="crg-body" id="GuideEmailIntro-{{ section.id }}">{{ 'creatine_guide.email.body' | t }}</p>
    {% form 'customer', id: email_form_id, class: 'crg-email-form' %}
      <input type="hidden" name="contact[tags]" value="newsletter,creatine-guide,chatgpt-deal-reminder">
      {% if form.posted_successfully? %}<p class="crg-email-status" role="status" data-email-status tabindex="-1">{{ 'creatine_guide.email.success' | t }}</p>{% else %}
        {% if form.errors %}<div class="crg-email-status" role="alert" data-email-status tabindex="-1">{{ form.errors | default_errors }}</div>{% endif %}
        <label for="GuideEmailInput-{{ section.id }}">{{ 'creatine_guide.email.label' | t }}</label>
        <input id="GuideEmailInput-{{ section.id }}" type="email" name="contact[email]" autocomplete="email" autocapitalize="off" required value="{{ form.email | escape }}" placeholder="{{ 'creatine_guide.email.placeholder' | t }}">
        <p class="crg-fine crg-email-consent">{{ 'creatine_guide.email.consent' | t }}{% if shop.privacy_policy.body != blank %} <a href="{{ shop.privacy_policy.url }}">{{ 'creatine_guide.footer.privacy' | t }}</a>{% endif %}</p>
        <button class="crg-button" type="submit">{{ 'creatine_guide.email.submit' | t }} <span aria-hidden="true">↗</span></button>
      {% endif %}
    {% endform %}
  </dialog>
  <noscript><p class="crg-wrap crg-fine">{{ 'creatine_guide.email.nojs' | t }}</p></noscript>
  <footer class="crg-footer crg-wrap"><div><a class="crg-logo" href="{{ routes.root_url }}">{{ 'creatine_guide.nav.brand' | t }}</a><span>{{ 'creatine_guide.footer.guide' | t }}</span></div><nav aria-label="{{ 'creatine_guide.footer.guide' | t }}">{% if shop.privacy_policy.body != blank %}<a href="{{ shop.privacy_policy.url }}">{{ 'creatine_guide.footer.privacy' | t }}</a>{% endif %}{% if shop.shipping_policy.body != blank %}<a href="{{ shop.shipping_policy.url }}">{{ 'creatine_guide.footer.shipping' | t }}</a>{% endif %}{% if shop.refund_policy.body != blank %}<a href="{{ shop.refund_policy.url }}">{{ 'creatine_guide.footer.returns' | t }}</a>{% endif %}<a href="mailto:{{ shop.email | escape }}">{{ 'creatine_guide.footer.contact' | t }}</a></nav><p>{{ 'creatine_guide.footer.disclaimer' | t }}</p></footer>
  <div class="crg-sticky" hidden><span>{{ 'creatine_guide.offer.sticky' | t }}</span><a class="crg-button" href="#GuideOffer-{{ section.id }}">{% if offer_active %}{{ 'creatine_guide.offer.sticky_offer' | t }}{% else %}{{ 'creatine_guide.offer.sticky_button' | t }}{% endif %}<span aria-hidden="true">↗</span></a></div>
</div>
{% stylesheet %}
.crg { --crg-blue: #0066a6; --crg-ink: #2e1206; --crg-paper: #fff8f0; color: var(--crg-ink); background: var(--crg-paper); font-family: var(--body-font); font-weight: 400; overflow: clip; }
.crg *, .crg *::before, .crg *::after { box-sizing: border-box; }
.crg [hidden] { display: none !important; }
.crg-wrap { width: min(1240px, calc(100% - 96px)); margin-inline: auto; }
.crg-space { padding-block: 100px; }
.crg h1, .crg h2, .crg h3 { font-family: var(--body-font); font-weight: 700; text-wrap: balance; }
.crg h1 { font-size: clamp(48px, 5.2vw, 76px); line-height: 1.02; letter-spacing: -.055em; max-width: 600px; margin: 28px 0 24px; }
.crg h2 { font-size: clamp(32px, 3.3vw, 48px); line-height: 1.12; letter-spacing: -.045em; }
.crg h3 { font-size: 22px; line-height: 1.25; letter-spacing: -.025em; }
.crg p { margin: 0; }
.crg a { text-underline-offset: 5px; }
.crg a:hover { text-decoration: underline; }
.crg a:focus-visible, .crg button:focus-visible, .crg summary:focus-visible, .crg select:focus-visible { outline: 3px solid currentColor; outline-offset: 5px; }
.crg [id] { scroll-margin-top: 30px; }
.crg-eyebrow { font-size: 11px; font-weight: 800; letter-spacing: .13em; text-transform: uppercase; line-height: 1.7; margin-bottom: 20px !important; display: flex; align-items: center; gap: 9px; }
.crg-dot { width: 7px; height: 7px; border-radius: 100%; background: #c43a16; }
.crg-body { font-size: 17px; line-height: 1.75; margin-top: 22px !important; max-width: 610px; }
.crg-muted { color: #624d43; line-height: 1.7; max-width: 350px; font-size: 16px; }
.crg-fine { font-size: 12px; line-height: 1.7; }
.crg-welcome { text-align: center; background: #e8f0f4; color: #214f6b; padding: 11px 20px; font-size: 12px; font-weight: 600; }
.crg-welcome a { display: block; }
.crg-welcome span { padding-left: 10px; }
.crg-nav { display: flex; align-items: center; justify-content: space-between; padding-block: 24px; gap: 24px; border-bottom: 1px solid #2e12061a; }
.crg-logo { font-family: var(--brand-font); font-size: 31px; letter-spacing: .04em; line-height: 1; }
.crg-nav nav { display: flex; gap: 32px; font-size: 13px; }
.crg-nav-cta { font-weight: 700; font-size: 13px; display: flex; align-items: center; gap: 24px; }
.crg-hero { display: grid; grid-template-columns: .95fr 1.05fr; align-items: center; gap: 30px; padding-block: 44px 52px; }
.crg-hero-lead { font-size: 22px; line-height: 1.45; margin-bottom: 13px !important; }
.crg-hero-media { position: relative; margin: 0; height: 580px; border-radius: 160px 160px 8px 8px; overflow: hidden; background: #ede8db; }
.crg-hero-media img { height: 100%; width: 100%; object-fit: cover; object-position: 75% center; }
.crg-hero-media figcaption { position: absolute; bottom: 18px; left: 16px; right: 16px; width: fit-content; max-width: calc(100% - 32px); padding: 8px 13px; margin: auto; font-size: 10px; background: #fff8f0ed; border-radius: 4px; letter-spacing: .035em; }
.crg-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 24px; margin-top: 30px; }
.crg-button { display: inline-flex; align-items: center; justify-content: center; gap: 34px; min-height: 52px; padding: 14px 25px; border: 1px solid transparent; background: #ff5a2c; color: #2e1206; border-radius: 4px; font-size: 14px; font-weight: 800; line-height: 1.4; cursor: pointer; transition: background .18s, color .18s; }
.crg-button:hover { background: #2e1206; color: #fff8f0; text-decoration: none !important; }
.crg-button:disabled { cursor: not-allowed; background: #ded6cf; color: #624d43; }
.crg-text-link { font-size: 13px; font-weight: 700; text-decoration: underline; }
.crg-facts { display: grid; grid-template-columns: repeat(3, 1fr); border-top: 1px solid #2e120625; padding-block: 25px; gap: 20px; }
.crg-facts span { display: flex; gap: 14px; align-items: center; justify-content: center; font-size: 13px; }
.crg-facts b { color: var(--crg-blue); font-size: 21px; font-weight: 600; }
.crg-split { display: grid; grid-template-columns: 1fr 1fr; gap: 90px; align-items: start; }
.crg-learn { margin-top: 10px; background: #eaf2f6; }
.crg-definition { border-left: 1px solid #0066a638; padding: 0 0 0 48px; }
.crg-definition p { margin-top: 18px; font-size: 17px; line-height: 1.75; }
.crg-orbit { background: #0066a6; color: white; display: grid; place-items: center; width: 65px; height: 65px; border-radius: 100%; font-size: 34px; margin-bottom: 28px; }
.crg-benefits { display: grid; grid-template-columns: repeat(3,1fr); gap: 36px; margin-top: 65px; }
.crg-benefits article { border-top: 1px solid #0066a64d; padding-top: 20px; }
.crg-number { font-size: 11px; letter-spacing: .1em; display: block; margin-bottom: 26px; color: #215472; }
.crg-benefits p { margin-top: 14px; font-size: 15px; line-height: 1.65; }
.crg-learn-note { margin-top: 30px !important; color: #3c5665; max-width: 800px; }
.crg-science .crg-body { margin-top: 0 !important; }
.crg-details { border-bottom: 1px solid #2e12062e; }
.crg-details summary { display: flex; align-items: center; justify-content: space-between; gap: 20px; list-style: none; cursor: pointer; padding: 21px 0; font-size: 15px; font-weight: 700; line-height: 1.5; }
.crg-details summary::-webkit-details-marker { display: none; }
.crg-details summary span { flex-shrink: 0; font-size: 23px; line-height: 1; font-weight: 400; transition: transform .15s; }
.crg-details[open] summary span { transform: rotate(45deg); }
.crg-details > div { padding-bottom: 24px; font-size: 15px; line-height: 1.8; }
.crg-details > div p + p { margin-top: 15px; }
.crg-details ul { padding-left: 20px; margin: 0 0 20px; }
.crg-details li + li { margin-top: 12px; }
.crg-details li a { text-decoration: underline; }
.crg-comparison { background: #f2e9df; }
.crg-section-intro { max-width: 670px; }
.crg-table { width: 100%; border-collapse: collapse; table-layout: fixed; margin-top: 42px; font-size: 14px; line-height: 1.6; text-align: left; }
.crg-table th, .crg-table td { padding: 22px 25px; vertical-align: middle; border-bottom: 1px solid #2e120624; }
.crg-table th { font-weight: 600; }
.crg-table thead th { font-size: 12px; font-weight: 800; }
.crg-table td:nth-child(2), .crg-table th:nth-child(2) { background: #fff8f0; }
.crg-table thead th:nth-child(2) { background: #0066a6; color: white; }
.crg-table tbody th { padding-left: 0; }
.crg-table thead th:first-child { padding-left: 0; }
.crg-explain { gap: 50px; margin-top: 26px; }
.crg-proof-links { display: flex; flex-wrap: wrap; gap: 25px; margin-top: 30px; }
.crg-label { background: #fff; border: 1px solid #2e12062a; padding: 32px 38px; border-radius: 4px; }
.crg-label > div { display: flex; flex-wrap: wrap; align-items: baseline; gap: 8px 20px; justify-content: space-between; padding-block: 20px; border-top: 1px solid #2e12061a; }
.crg-label strong { font-size: 42px; letter-spacing: -.05em; font-weight: 600; color: var(--crg-blue); }
.crg-label small { font-size: 16px; letter-spacing: 0; }
.crg-label span { font-size: 12px; }
.crg-label .crg-fine { margin-top: 12px; }
.crg-reviews { margin-top: 65px; }
.crg-review-grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(240px,1fr)); gap: 24px; }
.crg-review-grid figure { margin: 20px 0 0; background: #f2e9df; padding: 25px; }
.crg-review-grid blockquote { margin: 0 0 20px; font-size: 18px; }
.crg-review-grid figcaption { font-size: 13px; font-weight: 700; }
.crg-faq h2 { max-width: 350px; }
.crg-faq .crg-details:first-child summary { padding-top: 0; }
.crg-offer { background: #f2e9df; }
.crg-offer-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 70px; align-items: center; }
.crg-offer-image { border-radius: 8px; overflow: hidden; background: #e7e0d6; }
.crg-offer-image img { width: 100%; height: auto; }
.crg-offer-copy .crg-body { margin-top: 15px !important; margin-bottom: 28px; }
.crg-offer-copy h3 { font-size: 18px; margin-bottom: 8px; }
.crg-offer-badge { display: inline-block; background: #0066a6; color: white; border-radius: 3px; padding: 9px 12px; font-size: 12px; font-weight: 700; margin-bottom: 22px !important; }
.crg-price { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; margin: 18px 0 7px; }
.crg-price strong { font-size: 32px; font-weight: 700; letter-spacing: -.03em; }
.crg-price s { font-size: 19px; color: #624d43; }
.crg-one-time { font-size: 13px; margin-bottom: 20px !important; }
.crg-buy { width: 100%; margin-top: 6px; }
.crg-product-form label { display: block; font-size: 13px; font-weight: 700; margin-bottom: 8px; }
.crg-product-form select { width: 100%; padding: 14px; font: inherit; border: 1px solid #2e12065c; background: #fff8f0; color: inherit; border-radius: 4px; margin-bottom: 12px; }
.crg-terms { margin-top: 15px !important; color: #624d43; }
.crg-shipping { margin: 12px 0 18px !important; }
.crg-footer { padding-block: 45px 110px; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 30px; }
.crg-footer > div { display: flex; gap: 25px; align-items: center; }
.crg-footer > div span { font-size: 12px; }
.crg-footer nav { display: flex; gap: 25px; font-size: 12px; }
.crg-footer > p { border-top: 1px solid #2e120625; padding-top: 25px; width: 100%; font-size: 11px; line-height: 1.7; max-width: 850px; }
.crg-sticky { position: fixed; bottom: 16px; left: 50%; transform: translateX(-50%); width: min(610px,calc(100% - 32px)); z-index: 25; display: flex; align-items: center; justify-content: space-between; gap: 15px; padding: 10px 10px 10px 22px; background: #fff8f0; border: 1px solid #2e120625; border-radius: 7px; box-shadow: 0 5px 30px #2e12061a; }
.crg-sticky > span { font-size: 13px; font-weight: 700; }
.crg-sticky .crg-button { min-height: 44px; font-size: 12px; gap: 16px; padding: 12px 18px; }
@media (min-width: 1600px) { .crg-hero { gap: 65px; } }
@media (max-width: 1000px) { .crg-wrap { width: calc(100% - 56px); } .crg-split { gap: 40px; } .crg-hero-media { height: 530px; border-radius: 110px 110px 6px 6px; } .crg-definition { padding-left: 30px; } .crg-offer-grid { gap: 40px; } }
@media (max-width: 749px) {
  .crg-wrap { width: calc(100% - 40px); } .crg-space { padding-block: 60px; } .crg-nav { padding-block: 20px; } .crg-nav nav { display: none; } .crg-logo { font-size: 27px; } .crg-nav-cta { font-size: 12px; gap: 15px; }
  .crg-welcome { font-size: 11px; padding: 10px 20px; } .crg-hero { grid-template-columns: 1fr; padding-block: 34px 25px; gap: 28px; } .crg h1 { font-size: clamp(46px,12vw,68px); max-width: 510px; margin: 18px 0; } .crg-eyebrow { font-size: 10px; margin-bottom: 16px !important; } .crg-hero-lead { font-size: 20px; } .crg-muted { font-size: 15px; max-width: 380px; } .crg-actions { margin-top: 24px; gap: 25px; } .crg-hero-media { height: 320px; border-radius: 90px 90px 6px 6px; } .crg-hero-media img { object-position: 75% 65%; }
  .crg-facts { gap: 10px; padding-block: 20px; } .crg-facts span { flex-direction: column; gap: 4px; text-align: center; font-size: 10px; line-height: 1.45; } .crg-facts b { font-size: 18px; } .crg-split { grid-template-columns: 1fr; gap: 30px; } .crg-body { font-size: 16px; line-height: 1.75; } .crg-definition { padding: 25px; border: 1px solid #0066a633; } .crg-definition p { font-size: 15px; } .crg-orbit { width: 44px; height: 44px; font-size: 24px; margin-bottom: 20px; } .crg-benefits { grid-template-columns: 1fr; gap: 24px; margin-top: 32px; } .crg-benefits article { position: relative; padding-left: 38px; } .crg-number { position: absolute; left: 0; top: 25px; } .crg-benefits h3 { font-size: 20px; } .crg-benefits p { margin-top: 10px; }
  .crg-table { margin-top: 30px; font-size: 12px; line-height: 1.6; } .crg-table th, .crg-table td { padding: 15px 10px; overflow-wrap: anywhere; } .crg-table thead th { font-size: 11px; } .crg-table th:first-child { width: 27%; } .crg-table th:nth-child(2) { width: 34%; } .crg-explain { gap: 0; margin-top: 12px; } .crg-details summary { font-size: 14px; } .crg-label { padding: 25px; } .crg-label strong { font-size: 36px; }     .crg-offer-grid { grid-template-columns: 1fr; gap: 35px; } .crg-offer-image { max-width: 420px; margin: auto; } .crg-footer { gap: 26px; padding-top: 35px; } .crg-footer > div { gap: 20px; } .crg-footer nav { gap: 22px; } .crg-sticky { bottom: max(10px,env(safe-area-inset-bottom)); padding-left: 13px; } .crg-sticky > span { font-size: 11px; max-width: 100px; } .crg-sticky .crg-button { gap: 12px; padding-inline: 14px; }
}
@media (prefers-reduced-motion: reduce) { .crg *, .crg *::before, .crg *::after { transition: none !important; scroll-behavior: auto !important; } }

.crg-space { padding-block: 76px; }
.crg-learn .crg-split { align-items: center; gap: 65px; }
.crg-lifestyle { margin: 0; position: relative; border-radius: 6px 95px 6px 6px; overflow: hidden; background: #e0eaf0; }
.crg-lifestyle img { display: block; width: 100%; aspect-ratio: 4 / 3; height: auto; object-fit: cover; }
.crg-lifestyle figcaption { position: absolute; bottom: 15px; left: 15px; padding: 8px 12px; font-size: 11px; color: #2e1206; background: #fff8f0ee; border-radius: 3px; }
.crg-benefits { margin-top: 35px; gap: 35px; }
.crg-number { margin-bottom: 14px; }
.crg-benefits p { margin-top: 9px; }
.crg-choice-tiles { display: grid; grid-template-columns: repeat(3,1fr); gap: 20px; margin-block: 30px; }
.crg-choice-tiles article { background: #fff8f0; border: 1px solid #2e12061a; border-radius: 5px; padding: 26px; }
.crg-choice-tiles article > span { display: grid; place-items: center; width: 52px; height: 52px; background: #eaf2f6; color: #0066a6; border-radius: 50%; font-size: 23px; font-weight: 700; margin-bottom: 23px; }
.crg-choice-tiles h3 { font-size: 21px; }
.crg-choice-tiles p { font-size: 13px; margin-top: 8px; }
.crg-table-details .crg-table { margin-top: 0; }
.crg-facts-disclosure img { max-width: 650px; width: 100%; height: auto; background: white; }
.crg-explain { margin-top: 0; }
@media (max-width: 1000px) {   .crg-learn .crg-split { gap: 35px; } }
@media (max-width: 749px) {
  .crg-space { padding-block: 48px; }
  .crg-lifestyle { border-radius: 5px 60px 5px 5px; }
  .crg-lifestyle figcaption { font-size: 10px; bottom: 10px; left: 10px; }
  .crg-benefits { gap: 18px; margin-top: 28px; }
  
  
  
  
  
  
  
  
  .crg-choice-tiles { grid-template-columns: repeat(3,1fr); gap: 8px; margin-block: 23px; }
  .crg-choice-tiles article { padding: 15px 10px; }
  .crg-choice-tiles article > span { width: 38px; height: 38px; margin-bottom: 18px; font-size: 19px; }
  .crg-choice-tiles h3 { font-size: 16px; }
  .crg-choice-tiles p { font-size: 11px; line-height: 1.5; }
  
  
  
  
  
  
}
.crg-tree-intro { max-width: 700px; }
.crg-tree-intro .crg-body { margin-top: 16px !important; }
.crg-tree-interactive { margin-top: 32px; }
.crg-tree-choices { display: grid; grid-template-columns: repeat(4,1fr); gap: 14px; }
.crg-tree-choices button { min-height: 165px; padding: 22px; background: #fff; border: 1px solid #8d969b; border-radius: 6px; text-align: left; display: flex; flex-direction: column; align-items: flex-start; gap: 16px; font-size: 17px; font-weight: 700; line-height: 1.35; position: relative; }
.crg-tree-choices button:hover { background: #eaf2f6; border-color: #0066a6; }
.crg-tree-choices button[aria-pressed="true"] { background: #0066a6; color: #fff; border-color: #0066a6; }
.crg-tree-icon { display: grid; place-items: center; border: 1px solid currentColor; border-radius: 50%; width: 37px; height: 37px; font-size: 23px; font-weight: 400; }
.crg-tree-arrow { position: absolute; top: 24px; right: 20px; font-size: 15px; }
.crg-tree-connector { width: 76%; height: 24px; margin: 0 auto 24px; border: 1px solid #0066a65e; border-top: 0; position: relative; }
.crg-tree-connector::after { content: ''; position: absolute; top: 100%; left: 50%; height: 24px; width: 1px; background: #0066a65e; }
.crg-tree-result { border: 1px solid #0066a633; border-radius: 7px; background: #eaf2f6; padding: 35px; }
.crg-tree-placeholder { text-align: center; padding: 3px; }
.crg-tree-placeholder > span { display: inline-block; color: #0066a6; font-size: 28px; margin-bottom: 8px; }
.crg-tree-placeholder h3 { font-size: 23px; }
.crg-tree-placeholder p { font-size: 14px; margin-top: 12px; }
.crg-tree-answer { display: grid; grid-template-columns: 1.25fr 1fr; gap: 50px; }
.crg-tree-answer h3 { font-size: 32px; line-height: 1.15; }
.crg-tree-answer .crg-body { margin: 15px 0 20px !important; font-size: 16px; }
.crg-tree-answer aside { border-left: 1px solid #0066a647; padding-left: 30px; }
.crg-tree-answer aside p:not(.crg-eyebrow) { font-size: 14px; line-height: 1.75; margin-bottom: 20px; }
.crg-tree-answer .crg-eyebrow { color: #214f6b; }
.crg-tree-more { margin-top: 24px; display: flex; flex-wrap: wrap; align-items: center; gap: 12px 25px; }
.crg-tree-more > p { font-size: 13px; font-weight: 700; }
.crg-tree-more > div { display: flex; flex-wrap: wrap; gap: 10px; }
.crg-tree-more button { min-height: 44px; border: 1px solid #96877f; background: #fff8f0; padding: 10px 15px; border-radius: 30px; font-size: 12px; font-weight: 700; }
.crg-tree-more button[aria-pressed="true"] { color: #fff; background: #0066a6; border-color: #0066a6; }
.crg-tree-scope { color: #624d43; margin-top: 18px !important; }
.crg-versus { margin-top: 30px; }
.crg-versus td, .crg-versus th { width: 25%; }
.crg-versus-note { color: #624d43; margin-top: 18px !important; }
@media (max-width: 749px) {
 .crg-tree-choices { grid-template-columns: 1fr 1fr; gap: 10px; }
 .crg-tree-choices button { min-height: 150px; padding: 17px; font-size: 15px; }
 .crg-tree-icon { width: 32px; height: 32px; font-size: 20px; }
 .crg-tree-arrow { top: 20px; right: 14px; }
 .crg-tree-connector { width: 52%; }
 .crg-tree-result { padding: 24px 20px; }
 .crg-tree-answer { grid-template-columns: 1fr; gap: 25px; }
 .crg-tree-answer h3 { font-size: 27px; }
 .crg-tree-answer aside { border-left: 0; border-top: 1px solid #0066a647; padding: 20px 0 0; }
 .crg-tree-answer aside p:not(.crg-eyebrow) { margin-bottom: 12px; }
 .crg-tree-placeholder h3 { font-size: 21px; }
 .crg-tree-more { display: block; }
 .crg-tree-more > div { margin-top: 12px; gap: 8px; }
 .crg-tree-more button { font-size: 11px; padding-inline: 11px; }
 .crg-versus td, .crg-versus th, .crg-versus th:first-child, .crg-versus th:nth-child(2) { width: 25%; padding: 14px 7px; font-size: 11px; }
 .crg-versus td:nth-child(2) { font-weight: 700; }
}

.crg-hero { display:block; padding-block:36px 0; text-align:center; }
.crg-hero-copy { max-width:800px; margin:auto; }
.crg-hero .crg-eyebrow { justify-content:center; margin-bottom:12px!important; }
.crg h1 { max-width:780px; margin:0 auto 18px; font-size:clamp(36px,5vw,66px); line-height:1.08; }
.crg-hero-lead { font-size:17px; max-width:610px; margin-inline:auto!important; }
.crg-tree.crg-space { padding-block:8px 44px; }
.crg-tree-interactive { margin-top:18px; }
.crg-tree-choices button { aspect-ratio:1/1.15; min-height:0; padding:0; overflow:hidden; background:#26404c; border:0; border-radius:12px; color:white; isolation:isolate; }
.crg-tree-choices button img { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; z-index:-2; }
.crg-tree-choices button::after { content:''; position:absolute; inset:0; background:linear-gradient(transparent 32%,rgba(10,22,29,.92)); z-index:-1; }
.crg-tree-choices button[aria-pressed="true"] { outline:4px solid #0066a6; outline-offset:3px; }
.crg-card-copy { margin-top:auto; padding:20px; font-size:22px; line-height:1.16; max-width:95%; }
.crg-tree-arrow { top:12px; right:12px; width:28px; height:28px; display:grid; place-items:center; background:#fff8f0; color:#2e1206; border-radius:50%; }
.crg-tree-connector { display:none; }
.crg-tree-result { margin-top:22px; }
.crg-tree-result:has(.crg-tree-placeholder) { display:none; }
.crg-tree-scope { text-align:center; }
.crg-space { padding-block:60px; }
.crg-choice-tiles article { padding:22px; }
.crg-versus td,.crg-versus th { width:33.333%; }
.crg-comparison-sources { margin-top:10px!important; text-decoration:underline; }
@media(max-width:749px){
.crg-wrap { width:calc(100% - 32px); }
.crg-nav { padding-block:18px; }
.crg-hero { padding-top:26px; }
.crg h1 { font-size:38px; letter-spacing:-.045em; }
.crg-hero-lead { font-size:14px; line-height:1.5; }
.crg-hero .crg-eyebrow { font-size:10px; }
.crg-card-copy { padding:14px 12px; font-size:18px; }
.crg-tree-choices { gap:12px; }
.crg-tree-choices button { min-height:0; aspect-ratio:1/1.1; }
.crg-tree-result { padding:22px 18px; }
.crg-tree-answer { gap:16px; }
.crg-tree-answer .crg-body { font-size:15px; line-height:1.6; }
.crg-tree-answer aside { padding-top:14px; }
.crg-tree-answer aside p:not(.crg-eyebrow) { font-size:12px; }
.crg-space { padding-block:38px; }
.crg-choice-tiles { grid-template-columns:1fr; gap:8px; }
.crg-choice-tiles article { padding:15px 18px; }
.crg-choice-tiles h3 { font-size:19px; }
.crg-choice-tiles p { font-size:13px; }
.crg-versus td,.crg-versus th,.crg-versus th:first-child,.crg-versus th:nth-child(2) { width:33.333%; padding:14px 8px; font-size:12px; overflow-wrap:break-word; }
.crg-offer-grid { gap:24px; }
.crg-offer-image { max-height:360px; }
.crg-offer-image img { object-fit:contain; }
}

.crg { padding-top:44px; }
.crg-welcome { position:fixed; top:0; left:0; width:100%; z-index:30; padding:0; }
.crg-welcome button { width:100%; min-height:44px; padding:10px 16px; border:0; background:transparent; color:inherit; font:inherit; cursor:pointer; }
.crg [id] { scroll-margin-top:65px; }
.crg-email-dialog { color:#2e1206; background:#fff8f0; border:0; border-radius:16px; width:min(480px,calc(100% - 32px)); max-height:calc(100dvh - 32px); overflow:auto; padding:38px 28px 28px; margin:auto; box-shadow:0 20px 80px #0004; font-family:var(--body-font); }
.crg-email-dialog::backdrop { background:#15242cb3; }
.crg-email-dialog h2 { font-size:34px; margin:0; }
.crg-email-dialog .crg-body { font-size:16px; line-height:1.6; margin-top:16px!important; }
.crg-email-close { position:absolute; top:4px; right:6px; width:44px; height:44px; font-size:30px; background:transparent; border:0; cursor:pointer; color:inherit; }
.crg-email-form { margin-top:24px; }
.crg-email-form label { display:block; font-size:14px; font-weight:700; margin-bottom:8px; }
.crg-email-form input[type=email] { display:block; width:100%; min-height:50px; padding:12px; border:1px solid #78685c; border-radius:5px; font:inherit; font-size:16px; background:white; color:#2e1206; }
.crg-email-form input:focus-visible { outline:3px solid #0066a6; outline-offset:3px; }
.crg-email-consent { margin-block:14px!important; }
.crg-email-form .crg-button { width:100%; }
.crg-email-status { padding:16px; border:1px solid #0066a6; background:#eaf2f6; line-height:1.6; }
body:has(.crg-email-dialog[open]) { overflow:hidden; }
.crg-versus tbody tr:first-child td { font-size:clamp(23px,3vw,38px); font-weight:800; letter-spacing:-.04em; }
.crg-versus tbody tr:first-child td:nth-child(2) { color:#0066a6; }
.crg-month { background:#fff8f0; }
.crg-month-scroll { overflow-x:auto; overscroll-behavior-x:contain; border-radius:8px; }
.crg-month-scroll img { display:block; width:100%; min-width:1100px; height:auto; }
.crg-month-scroll:focus-visible { outline:3px solid #0066a6; outline-offset:4px; }
.crg-month-hint { display:none; margin-bottom:12px!important; color:#624d43; }
.crg-month-transcript h3 { margin:22px 0 10px; font-size:20px; }
.crg-month-transcript p { margin-bottom:14px; }
@media(max-width:1199px) { .crg-month-hint { display:block; } }
.crg-timeline {display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin:32px 0 22px}
.crg-timeline article {padding:30px;border:1px solid #d9c7b8;border-radius:16px;background:#fffdf9}
.crg-day {font-size:36px;font-weight:800;letter-spacing:-.05em;color:#c4542c}
.crg-timeline h3 {font-size:23px;margin:24px 0 12px}
.crg-timeline p {font-size:15px;line-height:1.6}
.crg-progress {height:5px;background:#eee2d6;margin-top:18px;border-radius:8px;overflow:hidden}
.crg-progress i {height:100%;display:block;background:#c4542c}
.crg-clean {margin-top:36px;padding:28px;background:#e9f1f6;border-radius:16px}
.crg-clean h3 {font-size:27px;margin-bottom:18px}
.crg-clean>div {display:flex;flex-wrap:wrap;gap:15px 32px;font-size:15px}
.crg-clean span {color:#0066a6;font-weight:800}
@media(max-width:749px){.crg-timeline{grid-template-columns:1fr;gap:12px}.crg-timeline article{padding:24px}.crg-timeline h3{margin-top:18px}.crg-clean>div{display:grid;gap:12px}}
{% endstylesheet %}
{% javascript %}
(() => {
  const initialize = (scope) => {
    scope.querySelectorAll('.crg:not([data-guide-ready])').forEach((guide) => {
      guide.dataset.guideReady = 'true';
      let fitVisible = false;
      let refreshSticky = () => {};
      const published = new Set();
      const publish = (name) => {
        const shopify = window.Shopify;
        if (published.has(name) || shopify?.designMode) return;
        if (typeof shopify?.analytics?.publish !== 'function' || typeof shopify?.customerPrivacy?.analyticsProcessingAllowed !== 'function') return;
        if (!shopify.customerPrivacy.analyticsProcessingAllowed()) return;
        try {
          const result = shopify.analytics.publish(`olnian:${name}`, { guide: 'creatine' });
          if (result && typeof result.catch === 'function') result.catch(() => {});
          published.add(name);
        } catch {}
      };
      guide.querySelector('.crg-actions .crg-button')?.addEventListener('click', () => publish('creatine_guide_explored'));
      guide.querySelector('.crg-product-form')?.addEventListener('submit', () => publish('creatine_purchase_clicked'));
      const fit = guide.querySelector('[data-tree]');
      const result = guide.querySelector('[data-tree-result]');
      if (fit && result) {
        fit.hidden = false;
        guide.querySelectorAll('[data-tree-choice]').forEach((button) => {
          button.addEventListener('click', () => {
            const choice = button.dataset.treeChoice;
            const path = choice;
            const template = guide.querySelector(`[data-tree-template="${path}"]`);
            if (!template) return;
            guide.querySelectorAll('[data-tree-choice]').forEach((item) => {
              item.setAttribute('aria-pressed', String(item.dataset.treeChoice === choice));
            });
            result.replaceChildren(template.content.cloneNode(true));
            publish('creatine_guide_explored');
            guide.querySelector('[data-tree-more]').hidden = false;
            result.querySelector('h3').focus({ preventScroll: true });
            result.scrollIntoView({ behavior: window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'instant' : 'smooth', block: 'nearest' });
          });
        });
      }
      const sticky = guide.querySelector('.crg-sticky');
      const hero = guide.querySelector('.crg-hero');
      const offer = guide.querySelector('.crg-offer');
      if ('IntersectionObserver' in window && sticky && hero && offer) {
        let pastHero = false;
        let reachedOffer = false;
        refreshSticky = () => { sticky.hidden = !pastHero || reachedOffer || fitVisible; };
        const observer = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.target === fit) fitVisible = entry.isIntersecting;
            if (entry.target === hero) pastHero = entry.boundingClientRect.bottom < 0;
            if (entry.target === offer) {
              reachedOffer = entry.isIntersecting || entry.boundingClientRect.top < 0;
              if (entry.isIntersecting) publish('creatine_offer_viewed');
            }
          });
          refreshSticky();
        });
        observer.observe(hero);
        observer.observe(offer);
        if (fit) observer.observe(fit);
        document.addEventListener('shopify:section:unload', (event) => {
          if (event.target.contains(guide)) observer.disconnect();
        }, { once: true });
      }
      const emailDialog = guide.querySelector('.crg-email-dialog');
      if (emailDialog) {
        let emailOpener;
        const openEmail = () => {
          emailOpener = document.activeElement;
          if (!emailDialog.open) emailDialog.showModal();
          (emailDialog.querySelector('[data-email-status]') || emailDialog.querySelector('input[type="email"]'))?.focus();
        };
        guide.querySelectorAll('[data-email-open]').forEach(button => button.addEventListener('click', openEmail));
        emailDialog.querySelector('[data-email-close]').addEventListener('click', () => emailDialog.close());
        emailDialog.addEventListener('click', event => {
          if (event.target !== emailDialog) return;
          const box = emailDialog.getBoundingClientRect();
          if (event.clientX < box.left || event.clientX > box.right || event.clientY < box.top || event.clientY > box.bottom) emailDialog.close();
        });
        emailDialog.addEventListener('close', () => emailOpener?.focus());
        if (emailDialog.querySelector('[data-email-status]')) openEmail();
      }
      const variants = guide.querySelector('[data-guide-variant]');
      if (variants) variants.addEventListener('change', () => {
        const option = variants.selectedOptions[0];
        const price = guide.querySelector('[data-guide-price]');
        const original = guide.querySelector('[data-guide-original]');
        const serving = guide.querySelector('[data-guide-serving]');
        if (price) price.textContent = option.dataset.price;
        if (original) original.textContent = option.dataset.original;
        if (serving) serving.textContent = option.dataset.serving;
      });
    });
  };
  initialize(document);
  document.addEventListener('shopify:section:load', (event) => initialize(event.target));
})();
{% endjavascript %}
{% schema %}
{
  "name": "Creatine guide",
  "tag": "section",
  "class": "creatine-guide-section",
  "limit": 1,
  "enabled_on": {
    "templates": [
      "page"
    ]
  },
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Creatine product",
      "info": "Use Premium Creatine Monohydrate, 281 g. Facts on this page are specific to this formula."
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Headline override",
      "info": "Leave blank to use the translated campaign headline."
    },
    {
      "type": "image_picker",
      "id": "timeline_image",
      "label": "First 30 days graphic"
    },
    {
      "type": "image_picker",
      "id": "strength_image",
      "label": "Digestion choice image"
    },
    {
      "type": "image_picker",
      "id": "active_image",
      "label": "Immune choice image"
    },
    {
      "type": "image_picker",
      "id": "plant_image",
      "label": "Recovery choice image"
    },
    {
      "type": "image_picker",
      "id": "focus_image",
      "label": "Energy choice image"
    },
    {
      "type": "image_picker",
      "id": "offer_image",
      "label": "Offer product image"
    },
    {
      "type": "image_picker",
      "id": "lifestyle_image",
      "label": "Daily routine photograph"
    },
    {
      "type": "image_picker",
      "id": "facts_image",
      "label": "Supplement facts image"
    },
    {
      "type": "header",
      "content": "First-order offer"
    },
    {
      "type": "paragraph",
      "content": "Create and test the matching discount in Shopify before enabling. Restrict it to first-time customers, one-time creatine purchases and one use per customer. Disable combinations. This section cannot create or validate discounts."
    },
    {
      "type": "checkbox",
      "id": "offer_enabled",
      "label": "Enable verified welcome offer",
      "default": false
    },
    {
      "type": "text",
      "id": "discount_code",
      "label": "Active Shopify discount code"
    },
    {
      "type": "range",
      "id": "discount_percent",
      "label": "First-order discount",
      "min": 5,
      "max": 50,
      "step": 5,
      "unit": "%",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Customer testimonial",
      "limit": 3,
      "settings": [
        {
          "type": "paragraph",
          "content": "Use only a real, permissioned review of this product. Do not add unsupported health claims or invented customers."
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Customer\u2019s exact words"
        },
        {
          "type": "text",
          "id": "author",
          "label": "Customer attribution"
        },
        {
          "type": "url",
          "id": "source_url",
          "label": "Original review link"
        },
        {
          "type": "text",
          "id": "source_label",
          "label": "Review link label",
          "default": "Read the review"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Creatine guide"
    }
  ]
}
{% endschema %}
